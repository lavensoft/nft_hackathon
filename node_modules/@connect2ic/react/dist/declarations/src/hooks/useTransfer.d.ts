declare type Props = {
    amount: number;
    to: string;
    from?: string;
};
export declare const useTransfer: ({ amount, to, from }: Props) => readonly [() => Promise<void>, {
    readonly loading: boolean;
    readonly error: Error | undefined;
}];
export {};
